//Q:06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let companyList = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon";
let newCompanyList = companyList.split(",");

console.log(newCompanyList);
